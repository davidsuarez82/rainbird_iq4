{
  "config": {
    "step": {
      "user": {
        "title": "Rain Bird IQ4",
        "description": "Enter your Rain Bird IQ4 cloud account credentials.",
        "data": {
          "username": "Email address or account name",
          "password": "Password",
          "auth_channel": "Authentication channel"
        },
        "data_description": {
          "username": "If login fails with an AWS WAF challenge (HTTP 202) using your email, try your Rain Bird account name instead. This is the editable display name found under Account Settings on the IQ4 website (e.g. \u201cJohn Smith\u201d) \u2014 not the numeric \u201cAccount ID\u201d shown in the mobile app, which won't work here.",
          "auth_channel": "How the integration logs in. Leave on \"Web portal\" for most accounts. Choose \"Mobile app\" only if zone control fails with a 403 error (e.g. US free-tier accounts)."
        }
      },
      "select_controller": {
        "title": "Select controller",
        "description": "Multiple controllers were found on this account. Choose the one to add.",
        "data": {
          "satellite_id": "Controller"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect. Check your credentials and internet connection, then try again.",
      "no_controllers": "No controllers were found on this account.",
      "unknown": "An unexpected error occurred. See the logs for details."
    },
    "abort": {
      "already_configured": "This controller is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Rain Bird IQ4 options",
        "data": {
          "auth_channel": "Authentication channel",
          "scan_interval": "Real-time data poll interval (seconds)",
          "scan_interval_config": "Controller config poll interval (seconds)",
          "scan_interval_program": "Program poll interval (seconds)"
        },
        "data_description": {
          "auth_channel": "How the integration logs in. Leave on \"Web portal\" for most accounts. Choose \"Mobile app\" only if zone control fails with a 403 error (e.g. US free-tier accounts). Changing this reloads the integration."
        }
      }
    }
  },
  "selector": {
    "auth_channel": {
      "options": {
        "web": "Web portal (default) — works for most accounts",
        "app": "Mobile app — use if zone control fails with 403 (e.g. US free tier)"
      }
    }
  }
}
